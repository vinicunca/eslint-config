{
  "name": "@vinicunca/eslint-config-monorepo",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@8.8.0",
  "author": "praburangki<https://github.com/praburangki>",
  "scripts": {
    "lint": "eslint .",
    "test": "pnpm -r run test",
    "postinstall": "is-ci || pnpm -r run stub",
    "build:lib": "pnpm -C eslint build",
    "prepublish": "pnpm run build:lib",
    "deploy:docs": "pnpm run gen:meta && pnpm -C docs generate",
    "release": "pnpm -C eslint release",
    "gen:meta": "bun scripts/gen-meta.ts",
    "gen:types": "bun scripts/types.ts",
    "registry": "pnpm publish --access public --no-git-checks"
  },
  "devDependencies": {
    "@types/node": "^20.8.4",
    "@vinicunca/eslint-config": "workspace:*",
    "@vinicunca/perkakas": "^0.0.4",
    "bumpp": "^9.2.0",
    "bun": "^1.0.4",
    "eslint": "^8.51.0",
    "is-ci": "^3.0.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2",
    "vitest": "^0.34.6"
  },
  "publishConfig": {
    "directory": "eslint"
  },
  "pnpm": {
    "overrides": {
      "eslint": "^8.51.0"
    }
  }
}
